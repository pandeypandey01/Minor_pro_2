# Minor_pro_2
An automated Python data pipeline that cleans, parses, and analyzes raw personal transaction data. The project standardizes messy transaction text, runs category maps, flags anomalies using statistical z-scores, identifies behavioral spending profiles ("archetypes"), and generates both a comprehensive text-based report and full Matplotlib/Seaborn visual dashboards.
